package App;

public enum EstadoCivil {
    SOLTERO,
    CASADO,
    VIUDO,
    UNION_LIBRE,
    DIVORCIADO;

    public static EstadoCivil fromInt(int i) {
        EstadoCivil[] vals = values();
        if (i >= 0 && i < vals.length) return vals[i];
        return null;
    }
}
